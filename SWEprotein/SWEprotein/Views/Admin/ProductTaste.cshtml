@model IEnumerable<SWEprotein.Models.tbTaste>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Smak</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
</head>
<body>
    <p>
        @Html.ActionLink("Lägg till ny smak", "CreateProductTaste")
    </p>
     <table class="table table-condensed table-bordered">
        <tr>
            <th>
              Namn
            </th>
            
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.sTaste)
            </td>
           
            <td>
                @Html.ActionLink("Redigera", "EditProductTaste", new { id=item.iID }) |
               
                @Html.ActionLink("Ta bort", "DeleteProductTaste", new { id=item.iID })
            </td>
        </tr>
    }
    
    </table> <div>     @Html.ActionLink("Adminsidan", "Index")</div>
</body>
</html>
